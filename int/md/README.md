# Intermediate `md` Folder

## Canonical source

- `final/modules` is the canonical final module source set.
- `final/supplemental` holds final companion materials such as the Unit 23 portfolio marking note.

## Derived file

- `aw-int-all.md` is a regenerated combined snapshot built from `final/modules`.
- Do not edit `aw-int-all.md` directly.

## Archived material

- Historical source layers now live under `int/archive/md`.
- That archive preserves the former live module working set and the earlier split-unit baseline.

Edit the intermediate book in `final/modules` only.
