# Phase 3 Unit 12 Repair Plan

Source: `int/md/working/aw-int-all_0519.md`

Answer key: `int/md/working/aw-int-answer-key.md`

Unit: Unit 12. Writing Simple External Inquiries

Date: 2026-06-08

## Assessment Summary

Unit 12 is structurally stable at the visible-shell level:

- visible sequence is `A B C D E F G H`
- div balance is `17 opens / 17 closes`
- placeholders total `14`
- all placeholders have explicit `rows=N`
- no duplicate placeholder IDs were found
- no stale generic labels such as `Write Here`, `Use this box`, `Homework Draft`, or `Transfer Extension` remain

The remaining issues are Phase 3 quality issues rather than first-pass shell issues.

## Findings

### 1. `A. Warm-Up` is front-loaded with explanation

Current order:

1. `External Inquiry Basics`
2. `Which Inquiry Is Better?`

The learner comparison task should lead, with the concept framing supporting it afterward.

### 2. `A` contains redundant prompt signaling

The `Discussion:` label is unnecessary because the activity title and task already define the learner action.

### 3. `H. Homework & Extension` is acceptable as two distinct subactivities, but the answer key does not reflect that clearly

`Internal Note Before the Inquiry` and `Homework Task` are not duplicates:

- the first is an internal pre-inquiry framing task
- the second is the actual external inquiry email

That distinction is pedagogically useful and should stay. The repair need is answer-key clarity, not forced consolidation.

### 4. `G` and `H` answer-key guidance is too compressed

The answer key needs to reflect:

- the reflection prompts in `G`
- the internal-note purpose in the first `H` task
- the real external inquiry email purpose in the second `H` task

## Repair Plan

### Markdown source

1. Rework `A. Warm-Up` so the learner comparison task leads the section.
2. Move the concept framing so it supports the task instead of pre-empting it.
3. Remove the redundant `Discussion:` label and simplify the opening prompt flow.
4. Keep both `H` subactivities, but preserve their distinct functions clearly.

### Answer key

1. Update `A` naming if the opening activity title changes.
2. Expand `G` guidance so it reflects the real reflection prompts.
3. Expand `H` guidance so both the internal-note task and the external-inquiry homework task are explicitly covered.

## Verification Targets After Repair

- visible sequence remains `A B C D E F G H`
- `A. Warm-Up` opens with the learner comparison task
- the two `H` tasks remain distinct and readable
- placeholders remain inside the correct divs
- answer-key labels and guidance match the repaired unit
