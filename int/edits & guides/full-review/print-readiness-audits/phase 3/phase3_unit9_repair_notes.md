# Phase 3 Unit 9 Repair Notes

Source: `int/md/working/aw-int-all_0519.md`

Answer key: `int/md/working/aw-int-answer-key.md`

Unit: Unit 9. Internal Notices

Date: 2026-06-08

## Summary

Unit 9 already had a stable `A B C D E F G H` shell, so the repair focused on Phase 3 quality rather than first-pass structural normalization.

## Repairs Applied

- Reworked `A. Warm-Up` so the learner-facing comparison task now leads the section.
- Renamed the opening notice-comparison activity from `Which Notice Is Clearer?` to `Compare Two Notices`.
- Removed the redundant `Discussion:` prompt label and folded the task into one clearer instruction.
- Kept the short concept primer and notice checklist inside `A`, but repositioned them so they support the comparison task instead of pre-empting it.
- Reworded the `Quick Notice Check` lead-in so it functions as a usable review/writing checklist.
- Consolidated `H. Homework & Extension` from two overlapping transfer tasks into one unified `Real-World Notice Transfer` task.
- Replaced the old split `H` placeholders with one fuller response space for the consolidated transfer task.
- Updated the Unit 9 answer key so the guided-practice scenario matches the live manuscript and the `A`, `G`, and `H` guidance matches the repaired task design.

## Verification

- Visible heading sequence remains `ABCDEFGH`.
- Unit-level fenced div balance: `17 opens / 17 closes`.
- Unit placeholders: `14` total, all with explicit `rows=N`.
- Duplicate placeholder IDs: none found in Unit 9.
- Stale generic labels in Unit 9: none found.

## Carry-Forward Notes

- When a unit already has a correct visible shell, Phase 3 review still needs to test whether `A. Warm-Up` behaves like a true opening activity rather than a mini lesson.
- Transfer/homework layers should not keep two full productive tasks unless their functions are clearly distinct.
- Answer-key repair must stay tied to scenario-level manuscript changes, not only visible heading changes.
