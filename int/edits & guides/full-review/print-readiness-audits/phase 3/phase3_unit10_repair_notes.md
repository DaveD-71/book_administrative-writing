# Phase 3 Unit 10 Repair Notes

Source: `int/md/working/aw-int-all_0519.md`

Answer key: `int/md/working/aw-int-answer-key.md`

Unit: Unit 10. Explaining Problems Clearly

Date: 2026-06-08

## Summary

Unit 10 already had a stable `A B C D E F G H` shell, so the repair focused on Phase 3 quality rather than first-pass structural normalization.

## Repairs Applied

- Reworked `A. Warm-Up` so the learner task now leads the section.
- Repositioned the short concept primer so it supports the warm-up instead of front-loading explanation.
- Increased the last warm-up placeholder from `rows=1` to `rows=2` while rebuilding the opening block.
- Consolidated `H. Homework & Extension` from two overlapping transfer tasks into one unified `Real-World Problem Update` task.
- Updated the Unit 10 answer key so `G` and `H` guidance matches the repaired end-of-unit task design.

## Verification

- Visible heading sequence remains `ABCDEFGH`.
- Unit-level fenced div balance: `15 opens / 15 closes`.
- Unit placeholders: `17` total, all with explicit `rows=N`.
- Duplicate placeholder IDs: none found in Unit 10.
- Stale generic labels in Unit 10: none found.

## Carry-Forward Notes

- When a unit already has the correct visible shell, Phase 3 review still needs to test whether `A. Warm-Up` behaves like a true opening activity.
- End-of-unit transfer layers should not preserve two overlapping full writing tasks under `H`.
