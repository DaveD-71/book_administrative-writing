# Phase 3 Unit 15 Repair Notes

Source: `int/md/working/aw-int-all_0519.md`

Answer key: `int/md/working/aw-int-answer-key.md`

Unit: Unit 15. Maintaining Consistent Email Style

Date: 2026-06-08

## Summary

Unit 15 already had a stable `A B C D E F G H` shell, so the repair focused on Phase 3 quality rather than first-pass structural normalization.

## Repairs Applied

- Reworked `A. Warm-Up` so the learner inconsistency-identification task now leads the section.
- Repositioned the short concept primer so it supports the opening task instead of front-loading explanation.
- Kept both `H` subactivities because they serve distinct transfer functions: team-rule development plus guided revision, and real-message or scenario-based independent rewriting.
- Split the first `H` task into separate response spaces so the team rules and revised email are no longer forced into one compact box.
- Updated the Unit 15 answer key so `G` and `H` guidance matches the repaired task design and explicitly covers both `H` tasks.

## Verification

- Visible heading sequence remains `ABCDEFGH`.
- Unit-level fenced div balance remains stable.
- Unit placeholders all retain explicit `rows=N`.
- Duplicate placeholder IDs: none found in Unit 15.
- Stale generic labels in Unit 15: none found.

## Carry-Forward Notes

- When an extension task asks for two distinct outputs, use separate response spaces instead of one generic box, even if both outputs belong to the same visible `H` layer.
