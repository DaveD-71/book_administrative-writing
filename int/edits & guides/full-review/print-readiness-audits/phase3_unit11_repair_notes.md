# Phase 3 Unit 11 Repair Notes

Source: `int/md/working/aw-int-all_0519.md`

Answer key: `int/md/working/aw-int-answer-key.md`

Unit: Unit 11. Writing Simple Meeting Summaries

Date: 2026-06-08

## Summary

Unit 11 already had a stable `A B C D E F G H` shell, so the repair focused on Phase 3 quality rather than first-pass structural normalization.

## Repairs Applied

- Reworked `A. Warm-Up` so the learner comparison task now leads the section.
- Renamed the opening comparison activity from `Which Summary Is Clearer?` to `Compare Two Summaries`.
- Removed the redundant `Discussion:` prompt label and folded the task into one clearer instruction.
- Repositioned the short concept primer and summary-quality explanation so they support the opening task instead of front-loading explanation.
- Consolidated `H. Homework & Extension` from two overlapping tasks into one unified `Real-World Summary Transfer` task.
- Updated the Unit 11 answer key so `A`, `G`, and `H` guidance matches the repaired task design.

## Verification

- Visible heading sequence remains `ABCDEFGH`.
- Unit-level fenced div balance remains stable.
- Unit placeholders retain explicit `rows=N`.
- Duplicate placeholder IDs: none found in Unit 11.
- Stale generic labels in Unit 11: none found.

## Carry-Forward Notes

- When a unit already has the correct visible shell, Phase 3 review still needs to test whether `A. Warm-Up` behaves like a true opening activity.
- End-of-unit transfer layers should not preserve two overlapping full writing tasks under `H`.
